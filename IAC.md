# IaC Compliance Review Findings

Reviewed both templates in full (1646 lines / 17 buckets in `vbps-inf-buckets`; 1636 lines / 20 tables in `vbps-inf-dynamo-inibited`) against the checklist. Both stacks are well-built overall: every S3 bucket and DynamoDB table has explicit `DeletionPolicy`/`UpdateReplacePolicy: Retain` (rule 21 — no violations), bucket/table resource policies use a sound deny-by-default pattern scoped to specific principal ARNs with `aws:SecureTransport` enforcement (rule 3 — no violations), all ARNs are built dynamically via `!Sub`/`${AWS::AccountId}`/`${AWS::Partition}` with no hardcoded account IDs (rule 27 — no violations), and there are no `Export` blocks to namespace-check (rule 28 — N/A). No Security Groups, IAM Roles, Lambda Functions, API Gateways, or Log Groups appear in either template, so rules 1, 2, 6, 9, 11–13, 15, 18, 20, 22, 24 are not applicable here.

Three genuine findings:


| Stack                    | File                                                         | Resource (logical ID or N/A)                                                                                              | Criticality | Rule # & Name          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| vbps-inf-buckets         | dev\_ops/aws/compiled/vbps-inf-buckets/template.yaml         | vbpsncilanding (line 1273)                                                                                                | Critical    | 4 — Malware Detection | This is an external-data landing bucket feeding a landing→quarantine→to-process→processed pipeline (vbpsnciquarantine, vbpsncitoprocess, vbpsnciprocessed), yet unlike vbpscontractuploads and vbpsmedia (each paired with an`AWS::GuardDuty::MalwareProtectionPlan` resource and a dedicated `nasco-pass/nasco-guardduty-s3malware` policy statement, lines 130-139 and 974-983), no MalwareProtectionPlan or GuardDuty role grant exists for the nci-landing bucket or the rest of its pipeline. |
| vbps-inf-buckets         | dev\_ops/aws/compiled/vbps-inf-buckets/template.yaml         | All 17`AWS::S3::Bucket` resources (e.g. vbpscontractuploads, vbpsmemories, vbpsreports, vbpsmedia, vbpsquarantine, etc.)  | Medium      | 17 — Tagging          | No`Tags` property is set on any S3 bucket in the template (confirmed via full-file search); buckets holding confidential/restricted data (contracts, settlement artifacts, member reports) also lack the required `classification` tag.                                                                                                                                                                                                                                                               |
| vbps-inf-dynamo-inibited | dev\_ops/aws/compiled/vbps-inf-dynamo-inibited/template.yaml | All 20`AWS::DynamoDB::Table` resources (e.g. vbpscontracts, vbpsapprovals, vbpssettlements, vbpsuseradditionaldata, etc.) | Medium      | 17 — Tagging          | No`Tags` property is set on any DynamoDB table in the template (confirmed via full-file search); tables clearly holding confidential/restricted data (contracts, settlements, approvals, user additional data) also lack the required `classification` tag.                                                                                                                                                                                                                                           |

No violations were found for rules 10 (log/S3 retention — no explicit Log Groups present; the two buckets with lifecycle rules, vbpsexportdynamo and vbpstemporary, correctly expire temp data after 1 day) or 14 (context boundaries — grouping all buckets/tables into dedicated infra stacks is a legitimate horizontal-shared-resource pattern per the checklist's own carve-out).

First audit result is in — I'll keep waiting for the other five before compiling the CSV.

## Audit Findings — evolve-inf-dynamo & evolve-inf-iam

Reviewed both compiled templates in full: `evolve-inf-dynamo/template.yaml` (22× `AWS::DynamoDB::Table`, no other resource types) and `evolve-inf-iam/template.yaml` (22× `AWS::IAM::Role`, no other resource types). No Lambda, API Gateway, Security Group, Log Group, RDS, or ACM resources exist in either file, so checklist rules 1, 6, 9, 10 (log groups), 11, 12, 13, 15, 18, 22, 24 have nothing to check against and are not reported. No `Outputs`/`Export` blocks exist in either file, so rule 28 is not applicable.


| Stack             | File                                                  | Resource (logical ID or N/A)                                                                                                 | Criticality | Rule # & Name                              | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| evolve-inf-iam    | dev\_ops/aws/compiled/evolve-inf-iam/template.yaml    | `nascoevolvemigrationrole` (nasco-evolve-migration-role), lines \~1291-1490                                                  | CRITICAL    | 2. Least Privilege — IAM Roles/Policies   | The`AssumeRolePolicyDocument` (lines 1303-1308) trusts both `lambda.amazonaws.com` **and** `redshift.amazonaws.com` to assume this role, even though the role's own policy is a standard Lambda `ExecuteLambdaPolicy` — the extra Redshift trust is unneeded and lets an additional AWS service assume a role holding broad cross-product DynamoDB/S3 permissions.                                                                                                                          |
| evolve-inf-iam    | dev\_ops/aws/compiled/evolve-inf-iam/template.yaml    | `nascoevolvecontractIngestionlambdarole` (nasco-evolve-contractIngestion-lambda-role), lines 1041-1049                       | CRITICAL    | 2. Least Privilege — IAM Roles/Policies   | `bedrock:InvokeModel`/`InvokeModelWithResponseStream` is granted on wildcarded `Resource: arn:aws:bedrock:*::foundation-model/*` (any region/any model), in addition to two already-scoped `!ImportValue` inference-profile ARNs on the same statement group — the wildcard entry is broader than needed given specific profiles are already referenced.                                                                                                                                    |
| evolve-inf-iam    | dev\_ops/aws/compiled/evolve-inf-iam/template.yaml    | `nascoevolvemigrationrole` (nasco-evolve-migration-role), lines 1315-1489                                                    | HIGH        | 8. Piggybacking                            | This single role is granted DynamoDB/S3/Redshift access spanning three unrelated product/context boundaries:`evolve*` tables, legacy snake\_case `vbps_*` tables/buckets, and legacy snake\_case `nexus_*` tables (e.g. `vbps_contracts`, `nexus_users`, `nexus_role_mapping`), plus two separate Redshift workgroups (`evolveRedshiftWorkgroupArn` and `nexusRedshiftWorkgroupArn`) — a single "migration" role reaching across Evolve, VBPS, and Nexus rather than one role per boundary. |
| evolve-inf-iam    | dev\_ops/aws/compiled/evolve-inf-iam/template.yaml    | `nascoevolvemigrationrole` (nasco-evolve-migration-role)                                                                     | MEDIUM      | 14. Context Boundaries                     | Same resource mixes resources/imports from three separate micro-service contexts (Evolve, VBPS, Nexus) in one role with no boundary separation, and its`Fn::ImportValue` on `nexusRedshiftWorkgroupArn` creates tight coupling between the Evolve IAM stack and the (apparently separate) Nexus stack.                                                                                                                                                                                       |
| evolve-inf-iam    | dev\_ops/aws/compiled/evolve-inf-iam/template.yaml    | All 22`AWS::IAM::Role` resources (e.g. `nascoevolvecontractsrole` … `nascoevolvedefaultrole`)                               | MEDIUM      | 17. Tagging                                | None of the 22 IAM Role resources have a`Tags` property at all.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| evolve-inf-dynamo | dev\_ops/aws/compiled/evolve-inf-dynamo/template.yaml | All 22`AWS::DynamoDB::Table` resources (e.g. `evolveUsers`, `evolveContracts`, `evolveSettlements` … `evolveNexusContract`) | MEDIUM      | 17. Tagging                                | None of the 22 tables have a`Tags` property; several (e.g. `evolveUsers`, `evolveSettlements`) plausibly hold confidential/PII or financial data and have no `classification` tag.                                                                                                                                                                                                                                                                                                           |
| evolve-inf-dynamo | dev\_ops/aws/compiled/evolve-inf-dynamo/template.yaml | All 22`AWS::DynamoDB::Table` resources                                                                                       | LOW         | 21. DeletionPolicy and UpdateReplacePolicy | No table resource sets`DeletionPolicy`/`UpdateReplacePolicy` (both default to `Delete`); `DeletionProtectionEnabled: true` mitigates but does not substitute for explicit CFN-level policies on these stateful resources.                                                                                                                                                                                                                                                                    |

Notable things checked but **not** flagged as violations:

* The `Resource: "*"` on `redshift-data:DescribeStatement`/`GetStatementResult`/`ListStatements` (appears repeatedly in evolve-inf-iam) is not a violation — these Redshift Data API actions do not support resource-level permissions per AWS, so `*` is required.
* The per-table `ResourcePolicy` documents on every DynamoDB table use an explicit deny-all-except-allowlisted-roles pattern (plus a deny for non-TLS traffic) — this correctly implements least-privilege resource policies (rule 3), not a broad allow-all.
* IAM Role names (`nasco-evolve-*-role`) and DynamoDB table names (`evolve*`) are correctly product-prefixed (rule 7) — no violation.
* No hardcoded account IDs/ARNs found; all account/region values use `!Sub`/pseudo-parameters, and cross-stack references use `!ImportValue` (rule 27) — no violation.

Second batch is in. Still waiting on four more agents before I compile the CSV.

**1 line of output**

**2 lines of output**

**2 lines of output**

## Audit Findings — vbps-inf-iam and vbps-inf-dynamo templates

Checklist loaded from `iac_checklist.md`. Both templates read in full:

* `dev_ops/aws/compiled/vbps-inf-iam/template.yaml` (1924 lines, \~27 `AWS::IAM::Role` resources)
* `dev_ops/aws/compiled/vbps-inf-dynamo/template.yaml` (1316 lines, 15 `AWS::DynamoDB::Table` resources)

No `AWS::Logs::LogGroup`, Lambda, API Gateway, Security Group, or RDS resources exist in either file, so rules 1, 6, 9, 11, 12, 13, 15, 18, 22, 24 are not applicable and are omitted (nothing to check). No `Outputs`/`Export` blocks exist in either template, so rule 28 doesn't apply.


| Stack           | File                                                | Resource                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Criticality | Rule # & Name                            | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| vbps-inf-iam    | dev\_ops/aws/compiled/vbps-inf-iam/template.yaml    | nascovbpscontractsrole (L74-91), nascovbpsapprovalsrole (L206-223), nascovbpsuploadingrole (L419-436), nascovbpsprovidersrole (L596-613), nascovbpsqualityMeasuresrole (L684-701), nascovbpsclaimsrole (L772-789), nascovbpsmembersrole (L826-842), nascovbpscontractAnalyticsrole (L913-930), nascovbpsfiltersrole (L1004-1021), nascovbpsanalyticsServicerole (L1092-1109), nascoevolveapprovalGrouprole (L1216-1233), nascovbpsflowrole (L1403-1420), nascovbpssettlementmiscrole (L1499-1516), nascovbpssettlementrole (L1568-1585), nascovbpsartifactsrole (L1664-1681), nascovbpsflowExecutionrole (L1760-1777) | Critical    | 2 - Least Privilege (IAM Roles/Policies) | Each role grants`redshift-data:ExecuteStatement` scoped to a specific workgroup (`!ImportValue nexusRedshiftWorkgroupArn`) in one statement, then grants the same action again unscoped (`Resource: "*"`) in the adjacent statement (alongside DescribeStatement/GetStatementResult/ListStatements, which do require `"*"`); since IAM Allow statements are additive, the role's effective `ExecuteStatement` permission is unrestricted, negating the resource restriction — `ExecuteStatement` should simply be dropped from the wildcard statement. |
| vbps-inf-iam    | dev\_ops/aws/compiled/vbps-inf-iam/template.yaml    | nascovbpsquarantinerole (L1295-1302), nascovbpscontractIngestionlambdarole (L1879-1886)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Critical    | 2 - Least Privilege (IAM Roles/Policies) | `bedrock:InvokeModel`/`InvokeModelWithResponseStream` is granted on `arn:aws:bedrock:*::foundation-model/*` (wildcard region AND model) plus `arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*` (wildcard on all inference profiles), instead of naming the specific foundation model(s)/profile the function actually invokes.                                                                                                                                                                                                     |
| vbps-inf-iam    | dev\_ops/aws/compiled/vbps-inf-iam/template.yaml    | N/A (all\~27 AWS::IAM::Role resources)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Medium      | 17 - Tagging                             | No`AWS::IAM::Role` resource in the template defines a `Tags` property (confirmed via full-file search), so none of the roles carry the required NASCO standard tags.                                                                                                                                                                                                                                                                                                                                                                                    |
| vbps-inf-dynamo | dev\_ops/aws/compiled/vbps-inf-dynamo/template.yaml | N/A (all 15 AWS::DynamoDB::Table resources, e.g. vbpscontracts, vbpssettlements, vbpsapprovals)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Medium      | 17 - Tagging                             | No`AWS::DynamoDB::Table` resource defines a `Tags` property; tables holding confidential/restricted contract, approval and settlement data also lack the required `classification` tag.                                                                                                                                                                                                                                                                                                                                                                 |

### Notable things checked and NOT flagged

* **Rule 21 (DeletionPolicy/UpdateReplacePolicy)**: All 15 DynamoDB tables in vbps-inf-dynamo correctly set `DeletionPolicy`/`UpdateReplacePolicy` via `!FindInMap [DynamoTable, Config, Policy]` (→ `Retain`). No violation.
* **Rule 7 (Name Spacing)**: All IAM roles use the `nasco-<product>-<name>-role` pattern consistently. No violation.
* **Rule 8 (Piggybacking)**: Roles are scoped one-per-microservice with no evidence of a single role serving unrelated purposes. No violation.
* **Rule 3 (Resource-based policies)**: The DynamoDB table `ResourcePolicy` blocks implement a well-formed default-deny/allowlist pattern (deny all non-listed principals, deny actions outside a role's intended verb set, deny non-TLS traffic). No broad allow-all statements found.
* Considered but excluded as too speculative for a compliance record: possible missing DynamoDB TTL on `vbpsflowexecutionenvironments`/`vbpsflowexecutionenvironmentmemories` (rule 10) — table purpose isn't clearly "time-limited" from the template alone.
* Also noticed (not a checklist rule, so not tabulated): the DynamoDB table `vbpsreport` (L837-885, `TableName: vbps_report`, singular) doesn't match the `vbps_reports` (plural) table ARN that `nascovbpsreportsrole` and several other IAM policies in vbps-inf-iam grant access to — likely a naming-drift bug between the two stacks, worth a follow-up outside this compliance review.

Third batch in — including a flagged IAM wildcard-permission escape pattern (scoped grant undermined by an adjacent unscoped statement). Three agents remain.

# IaC Compliance Audit Findings

Reviewed against `iac_checklist.md` (28 numbered rules). Table below lists only genuine, concrete violations found in the four target files.


| Stack                           | File                                                                                     | Resource (logical ID or N/A)                                                                                                                                           | Criticality | Rule # & Name                            | Description                                                                                                                                                                                                                    |
| ------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraECSTaskExecutionRole                                                                                                                                             | CRITICAL    | 2 — Least Privilege (IAM)               | Inline policy`RemoraExecutionAdditionalPermissions` grants `logs:CreateLogGroup` on `Resource: '*'` instead of a scoped log-group ARN (line \~114).                                                                            |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraECSTaskRole                                                                                                                                                      | CRITICAL    | 2 — Least Privilege (IAM)               | `cognito-idp:GetUser/ListUsers/AdminGetUser/AdminUpdateUserAttributes/AdminDeleteUser/AdminCreateUser` all use `Resource: '*'` (lines 175-182) instead of the specific User Pool ARN; Cognito supports resource-level scoping. |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraECSTaskRole                                                                                                                                                      | CRITICAL    | 2 — Least Privilege (IAM)               | `sqs:ReceiveMessage`/`sqs:DeleteMessage` use `Resource: '*'` (lines 196-199) with no SQS queue even defined in the template to scope to.                                                                                       |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | remoraInputBucket                                                                                                                                                      | CRITICAL    | 4 — Malware Detection                   | Bucket accepts external uploads (public CORS`PUT`/`GET`, `AllowOrigin: '*'`) with no GuardDuty S3 malware scanning configured.                                                                                                 |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | N/A (stack-wide)                                                                                                                                                       | HIGH        | 6 — Monitoring                          | No`AWS::CloudWatch::Alarm` resources exist anywhere in the stack (ECS Service, ALB target group, DocumentDB) — no alerting on failures.                                                                                       |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraLogGroupResource                                                                                                                                                 | HIGH        | 7 — Name Spacing                        | `LogGroupName: /ecs/-remora-worker` (line 73) has a stray leading dash — the product-name segment is missing/unsubstituted.                                                                                                   |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraDocumentDbCluster                                                                                                                                                | HIGH        | 9 — Recovery Support                    | DocDB cluster has no snapshot-ID parameter/Condition to allow restoring from a snapshot.                                                                                                                                       |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | N/A (stack-wide)                                                                                                                                                       | MEDIUM      | 17 — Tagging                            | No resource in the template (S3 buckets, IAM roles, ECS Cluster/Service, DocumentDB cluster, Secrets, ALB) has a`Tags` property.                                                                                               |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | e.g. remoraS3BucketPolicy, remoraInputBucketPolicy, remoraOutputBucketPolicy, remoraLogsBucketPolicy                                                                   | MEDIUM      | 27 — External/Reused Values             | Bucket policies hardcode the account-specific pipeline role ARN`arn:aws:iam::211125350567:role/nasco/nasco-ecr-remora-dev-pipeline` (e.g. line 521) repeated 4x instead of a Parameter.                                        |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraLogGroupResource, remoraRootPasswordMasterSecret, remoraAdminJwtMasterSecret, RemoraDocumentDbSecret, RemoraDocumentDbConnectionSecret, RemoraDocumentDbInstance | LOW         | 21 — DeletionPolicy/UpdateReplacePolicy | Stateful resources (log group, Secrets Manager secrets, DocDB instance) lack explicit`DeletionPolicy`/`UpdateReplacePolicy` (unlike the S3 buckets and DocDB cluster, which correctly set them).                               |
| remora                          | dev\_ops/aws/compiled/remora/template.yaml                                               | RemoraInternalLoadBalancerSecurityGroup, RemoraSecurityGroup, RemoraDocumentDbSecurityGroup                                                                            | LOW         | 22 — Empty SG Egress                    | None of the three Security Groups define an explicit`SecurityGroupEgress`, defaulting to CloudFormation's allow-all egress.                                                                                                    |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | vbpsExecuteFlow, vbpsStartDryRun, vbpsStartFlow                                                                                                                        | MEDIUM      | 15 — Incorrect Timeout                  | Each has`Timeout: 900` (lines 270, 476, 579) while attached to an API Gateway `Api` event — API Gateway's hard integration limit is 29s, so these will always time out at the gateway.                                        |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | N/A (all 7 functions)                                                                                                                                                  | LOW         | 19 — Code Readability (Globals)         | No`Globals` section despite `Runtime: nodejs22.x`, `Architectures: [arm64]`, and identical `VpcConfig` being repeated on every one of the 7 `AWS::Serverless::Function` resources.                                             |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | N/A (all 7 functions + vbpsSettlementApiGateway)                                                                                                                       | LOW         | 24 — Tracing                            | No Lambda has`Tracing: Active` and the API Gateway has no `TracingEnabled` — X-Ray is not enabled anywhere in the stack.                                                                                                      |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | N/A (all functions, log groups, alarms)                                                                                                                                | MEDIUM      | 17 — Tagging                            | No`Tags` property on any Lambda, Log Group, or Alarm resource.                                                                                                                                                                 |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | e.g. vbpsArtifactsServiceErrorsAlarm (representative of\~7 alarms)                                                                                                     | MEDIUM      | 27 — External/Reused Values             | SNS alarm ARN is hand-built via`!Sub arn:...:sns:...:api-alerts-topic` on every alarm instead of reusing the `!ImportValue vbpsApiAlertsTopicArn` already used correctly at line 145.                                          |
| vbps-settlement-lambdas         | dev\_ops/aws/compiled/vbps-settlement-lambdas/template.yaml                              | vbpsSettlementApiGateway (CognitoAuthorizer.UserPoolArn)                                                                                                               | LOW         | 19 — Code Readability                   | Builds the User Pool ARN with`Fn::Join` (line 103) where `Fn::Sub` would be clearer.                                                                                                                                           |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | vbpsExecuteAgents, vbpsFinish, vbpsGetExecutionResult, vbpsQualityAssurance, vbpsStartOrchestrator                                                                     | MEDIUM      | 15 — Incorrect Timeout                  | All 5 Lambda functions have`Timeout: 900` while attached to the API Gateway `Api` event source (e.g. lines 137, 240, 343, 446, 552) — exceeds the 29s API Gateway integration limit.                                          |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | vbpsContractIngestionApiGateway                                                                                                                                        | HIGH        | 11 — Throttling                         | `MethodSettings` (lines 63-68) has no `ThrottlingBurstLimit`/`ThrottlingRateLimit`, unlike the sibling settlement stack's API Gateway which sets both.                                                                         |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | N/A                                                                                                                                                                    | HIGH        | 6 — Monitoring                          | No CloudWatch Alarm exists for the API Gateway itself (the settlement stack has an equivalent`vbpsSettlementApiHighRequestRateAlarm`, this stack has none).                                                                    |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | N/A (all 5 functions)                                                                                                                                                  | LOW         | 19 — Code Readability (Globals)         | No`Globals` section despite identical `Runtime`, `Architectures`, and `VpcConfig` repeated across all 5 functions.                                                                                                             |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | N/A (all functions + API GW + vbpsContractIngestionStateMachine)                                                                                                       | LOW         | 24 — Tracing                            | No Lambda`Tracing: Active`, no API Gateway `TracingEnabled`, and the Step Functions state machine has no `Tracing: Enabled`.                                                                                                   |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | N/A (all functions, log groups, alarms, state machine)                                                                                                                 | MEDIUM      | 17 — Tagging                            | No`Tags` property on any Lambda, Log Group, Alarm, or the State Machine resource.                                                                                                                                              |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/template.yaml                      | vbpsContractIngestionApiGateway (CognitoAuthorizer.UserPoolArn)                                                                                                        | LOW         | 19 — Code Readability                   | Builds the User Pool ARN with`Fn::Join` (line 91) where `Fn::Sub` would be clearer.                                                                                                                                            |
| vbps-contract-ingestion-lambdas | dev\_ops/aws/compiled/vbps-contract-ingestion-lambdas/state\_machine/definition.asl.json | ExecuteAgents, QualityAssurance, Finish                                                                                                                                | HIGH        | Error Handling (task-specific check)     | None of the three`Task` states defines a `Retry` or `Catch` block for the `lambda:invoke` calls — any transient Lambda failure fails the whole execution with no retry/backoff or error path.                                 |

## Notes on scope

* `remora` is an ECS/Fargate + DocumentDB stack, not Lambda-based — rules specific to Lambda (10 implicit-log-group, 15, 18, 19, 24) were evaluated as not applicable there rather than violations; findings above reflect what IS applicable (IAM, tagging, monitoring, SG egress, DeletionPolicy).
* Both `vbps-*` stacks correctly use `arm64` architecture (rule 18) and `CodeSigningConfigArn` via import (rule 13) on every function — no violations for those rules, so they're omitted from the table.
* Explicit Log Groups in all three CFN templates have `RetentionInDays` set (14 in remora, 90 via Mapping in the vbps stacks) — rule 10 is satisfied everywhere and omitted.
* IAM Role *policy documents* for the vbps stacks are not defined in these templates (roles are referenced only by constructed ARN, presumably defined in a separate IAM boundary stack), so rule 2 could not be evaluated for those two stacks.

Fourth batch in with several real findings — including a 900s Lambda timeout behind an API Gateway integration (which caps at 29s) showing up in two stacks, plus IAM wildcard and missing-DR issues in `remora`. Two agents left.

## Audit Findings — vbps-os-lambdas

Reviewed the full 4,498-line template (`dev_ops/aws/compiled/vbps-os-lambdas/template.yaml`): 1 `AWS::Serverless::Api`, 42 `AWS::Serverless::Function`, 43 `AWS::Logs::LogGroup`, 127 `AWS::CloudWatch::Alarm`, plus custom-domain mapping resources. No IAM `Role`/`Policy` resources are defined in-template (all roles are referenced by ARN via `!Sub .../role/nasco-...`, defined in a separate IAM stack, so their policy *content* — rule 2 — is not verifiable from this file).

Compliant patterns worth noting (not flagged as violations): every Lambda has an explicit paired `LogGroup` with `RetentionInDays: 90` via `!FindInMap [ApiGatewayLogConfig, ...]` (rule 10 — pass), all 42 functions use `Architectures: [arm64]` (rule 18 — pass), all 42 have `CodeSigningConfigArn` (rule 13 — pass), and all 42 have Errors/Throttles/Duration `CloudWatch::Alarm`s wired to an SNS topic (rule 6 — pass). `vbpsScanS3File` is fed by a GuardDuty S3 Malware Protection EventBridge rule, satisfying rule 4 for the `vbps-media`/`vbps-contract-uploads` buckets.


| Stack           | File                                                | Resource                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Criticality | Rule # & Name                      | Description                                                                                                                                                                                                                                                                                                    |
| --------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | 18 Migration Lambdas (vbpsMigrationClean, vbpsMigrationCleanStaging, vbpsMigrationCreateFlow, vbpsMigrationCreateResource, vbpsMigrationDeleteFlow, vbpsMigrationDeleteResource, vbpsMigrationGetActiveFlow, vbpsMigrationGetAll, vbpsMigrationGetAllFlows, vbpsMigrationGetFlows, vbpsMigrationLoad, vbpsMigrationReadManifest, vbpsMigrationStagingInfo, vbpsMigrationStatus, vbpsMigrationStop, vbpsMigrationStopFlow, vbpsMigrationUnload, vbpsMigrationUpdateResource) | MEDIUM      | 15 — Incorrect Timeout            | Each has`Timeout: 900` while also having an `Events: Type: Api` (POST) trigger on `vbpsOSApiGateway`; API Gateway hard-caps synchronous integrations at 29s, so 900s is unreachable/misconfigured (contrast with the 23 Service Lambdas which correctly use `Timeout: 29` on their Api events).                |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | nasco-vbps-default-role, used by vbpsUsersService, vbpsWardenService, vbpsHealthCheck, vbpsPing, vbpsWardenGetLoginURL                                                                                                                                                                                                                                                                                                                                                      | HIGH        | 7/8 — Name Spacing & Piggybacking | One generically-named "default" role is shared across 5 functionally unrelated Lambdas (user management, Warden SSO, health check, ping, login-URL redirect), spanning distinct context boundaries with a single blast-radius IAM identity.                                                                    |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | nasco-evolve-migration-role, used by all 18 vbpsMigration\* functions                                                                                                                                                                                                                                                                                                                                                                                                       | HIGH        | 8 — Piggybacking                  | All 18 migration Lambdas share one IAM role whose name carries the`evolve` product prefix rather than `vbps`, suggesting it is reused from a different product's IAM boundary rather than scoped per-function/per-microservice.                                                                                |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | vbpsRolesService                                                                                                                                                                                                                                                                                                                                                                                                                                                            | MEDIUM      | 7 — Name Spacing                  | Role ARN is`nasco-evolve-approvalGroup-role` (product prefix `evolve`) even though the function belongs to the `vbps-os-lambdas` stack, indicating cross-product role reuse rather than a `nasco-vbps-...` scoped role.                                                                                        |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | vbpsOSApiGateway                                                                                                                                                                                                                                                                                                                                                                                                                                                            | HIGH        | 11 — Throttling                   | `MethodSettings` sets `MetricsEnabled`/`LoggingLevel` but no `ThrottlingBurstLimit`/`ThrottlingRateLimit`; the API has no throttle protection configured.                                                                                                                                                      |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | ALL Lambda functions (\~42) and vbpsOSApiGateway                                                                                                                                                                                                                                                                                                                                                                                                                            | LOW         | 24 — Tracing                      | No function sets`Tracing: Active`/`TracingConfig`, and the `Serverless::Api` has no `TracingEnabled`; X-Ray is absent stack-wide.                                                                                                                                                                              |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | ALL Lambda functions (\~42), 43 LogGroups, 127 Alarms, vbpsOSApiGateway                                                                                                                                                                                                                                                                                                                                                                                                     | MEDIUM      | 17 — Tagging                      | No resource sets an explicit`Tags` property anywhere in the template (only `PropagateTags: true` on the API); functions, log groups, alarms and the API Gateway all lack NASCO standard tags.                                                                                                                  |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | ALL Lambda functions (\~42); no Globals section present                                                                                                                                                                                                                                                                                                                                                                                                                     | LOW         | 19 — Code Readability             | Template has no`Globals:` section; identical `Runtime: nodejs22.x`, `Architectures: [arm64]`, `VpcConfig` (Subnets + `nexusSecurityGroupId`), and `CodeSigningConfigArn` values are repeated verbatim on every one of the 42 functions instead of being centralized.                                           |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | vbpsOSApiGateway (Auth.Authorizers.CognitoAuthorizer.UserPoolArn, line 98)                                                                                                                                                                                                                                                                                                                                                                                                  | LOW         | 19 — Code Readability             | Builds the Cognito user-pool ARN with`Fn::Join` (`!Join ["", ["arn:aws:cognito-idp:", ...]]`) where `Fn::Sub` would be clearer, per rule 19's explicit guidance.                                                                                                                                               |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | N/A (Conditions block, lines 6–13)                                                                                                                                                                                                                                                                                                                                                                                                                                         | MEDIUM      | 27 — External or Reused Values    | `nonProdAccount`/`prodAccount` Conditions hardcode 4 raw AWS Account IDs (730335180919, 242201270467, 667037895408, 872515256343) directly in the template instead of via a Parameter or Mapping.                                                                                                              |
| vbps-os-lambdas | dev\_ops/aws/compiled/vbps-os-lambdas/template.yaml | Whole stack (all 42 functions)                                                                                                                                                                                                                                                                                                                                                                                                                                              | MEDIUM      | 14 — Context Boundaries           | A single template/stack bundles\~20 largely unrelated microservice domains (migration ETL, analytics, claims, contracts, members, providers, quality measures, reports, users, Warden SSO, workflows, health checks) as 42 Lambda functions in one deployable unit rather than separate context-scoped stacks. |

**Files reviewed:**`C:\Projects\os\dev_ops\aws\compiled\vbps-os-lambdas\template.yaml`

Fifth batch in — the biggest stack (`vbps-os-lambdas`), with 18 more Lambdas hitting the same 900s-timeout-behind-API-Gateway bug, plus role piggybacking across product boundaries. One agent left, then I'll compile the CSV.
